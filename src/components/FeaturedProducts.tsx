import { Separator } from "@/components/ui/separator.tsx";

function FeaturedProducts() {
  return (
    <section>
      <h2 className={"text-4xl mb-8 font-bold"}>Featured Products</h2>
      <Separator />
    </section>
  );
}

export default FeaturedProducts;
